# Production changes — 2026-07-19

1. Audited the React, service, repository, authentication, and Supabase Edge Function structure.
2. Preserved existing functional dependencies instead of deleting files based on filename assumptions.
3. Added consistent Edge Function configuration files to folders that were missing them.
4. Added a production admin authorization layer based on verified Telegram sessions and `admin_users`.
5. Added an RLS-protected admin role table and audit-log table.
6. Added live admin dashboard and validated settings-update functions.
7. Added a lazy-loaded Arabic admin interface available through `?admin=1`.
8. Added deployment and security documentation.
9. Corrected CSS import order.
10. Verified the project with `npm run verify`.
